"""
export/

Simulator export (USD/IsaacSim, Gazebo/SDF, ...) is a separate, later
concern -- stubbed for now, not built this phase. See CLAUDE.md "Purpose"
and "Explicitly out of scope this phase". These subpackages exist only so
the intended layout is visible; they contain no generation or export
logic yet.
"""
